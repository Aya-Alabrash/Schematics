export interface Schema {
  /**
   * The name of the project.
   */
  project?: string;
  /**
   * The path to create the interface.
   */
  path?: string;
  /**
   * Specifies the name.
   */
  name: string;
}